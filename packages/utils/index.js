/**
 * Calculate sum of two number
 * @param {number} a
 * @param {number} b
 * @version __version__
 */
function sum(a, b) {
  return a + b;
}

/**
 * Returns subtraction
 * @param {number} a
 * @param {number} b
 */
function sub(a, b) {
  return a - b;
}

module.exports = {
  sum,
  sub,
};
